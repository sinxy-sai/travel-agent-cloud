services:
  agent-runtime:
    env_file:
      - ./agent-runtime/.env
    # Enable these when running `docker compose --profile worker --profile tools up --build`
    # and you want Agent Runtime to call the local travel-mcp stub.
    # environment:
    #   TRAVEL_TOOL_PROVIDER: fastmcp
    #   FASTMCP_BASE_URL: http://travel-mcp:8100/mcp

  agent-runtime-worker:
    env_file:
      - ./agent-runtime/.env

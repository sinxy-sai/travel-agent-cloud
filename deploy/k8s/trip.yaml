apiVersion: apps/v1
kind: Deployment
metadata:
  name: travel-trip
  namespace: travel-agent-cloud
  labels:
    app: travel-trip
spec:
  replicas: 1
  selector:
    matchLabels:
      app: travel-trip
  template:
    metadata:
      labels:
        app: travel-trip
    spec:
      containers:
        - name: travel-trip
          image: docker.io/sinxysai/travel-agent-cloud-trip:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8200
          env:
            - name: AGENT_RUNTIME_URL
              value: http://agent-runtime:8000
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: travel-trip-secrets
                  key: DATABASE_URL
                  optional: true
            - name: ALLOWED_ORIGINS
              value: "*"
            - name: TRIP_SERVICE_REQUEST_TIMEOUT_SECONDS
              value: "180"
            - name: OTEL_ENABLED
              value: "true"
            - name: OTEL_SERVICE_NAMESPACE
              value: travel-agent-cloud
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: http://tempo:4318
            - name: AUTH_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: travel-trip-secrets
                  key: AUTH_SECRET_KEY
                  optional: true
            - name: INTERNAL_SERVICE_TOKEN
              valueFrom:
                secretKeyRef:
                  name: travel-trip-secrets
                  key: INTERNAL_SERVICE_TOKEN
                  optional: true
          readinessProbe:
            httpGet:
              path: /health
              port: 8200
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8200
            initialDelaySeconds: 15
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: travel-trip
  namespace: travel-agent-cloud
spec:
  selector:
    app: travel-trip
  ports:
    - name: http
      port: 8200
      targetPort: 8200
